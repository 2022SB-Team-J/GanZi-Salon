version: '3'

services:
  frontend:
    build: ./frontend
    command: ["npm", "start"]
    ports:
      - 3000:3000
    volumes:
      - ./frontend:/app

      
  backend:
    build: ./backend
    ports:
      - 8000:8000
    volumes:
    - .:/user/src/gz-salon


#  ai:
#    build: ./ai_model
#    image: continuumio/miniconda3
#    ports:
#      - "8888:8888"
#    volumes:
#      - ai_data:/var/lib/ai
#    stdin_open: true
#    tty: true
#

  web-test:
    image: "nginx:latest"
  
 
volumes : 
  mysql_data:
  ai_data:


